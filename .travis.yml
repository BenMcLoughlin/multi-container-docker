sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t benmcloughlin/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true benmcloughlin/react-test npm test

after_success:
  - docker build -t benmcloughlin/multi-client ./client
  - docker build -t benmcloughlin/multi-nginx ./nginx
  - docker build -t benmcloughlin/multi-server ./server
  - docker build -t benmcloughlin/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push benmcloughlin/multi-client
  - docker push benmcloughlin/multi-nginx
  - docker push benmcloughlin/multi-server
  - docker push benmcloughlin/multi-worker
